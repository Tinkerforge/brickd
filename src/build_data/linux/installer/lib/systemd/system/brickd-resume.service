[Unit]
Description=Brick Daemon (Resume)
After=suspend.target,brickd.service
Requisite=brickd.service

[Service]
User=root
Type=oneshot
ExecStart=/usr/bin/pkill -F /var/run/brickd.pid -L -USR1
TimeoutSec=0
StandardOutput=syslog

[Install]
WantedBy=suspend.target
